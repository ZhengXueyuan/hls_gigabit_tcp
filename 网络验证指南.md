# 网络验证指南 (ETH1 / RTL8211E-VL)

> 2026-08-23 更新。✅ **全链打通并通过压测** — `py_net_test.py` (anaconda python)
> **7/7 全 PASS**: ping / UDP 64B / UDP 512B / TCP 25B / TCP 1608B / **TCP 2000B×3**。
> 当前板上位流 = `vivado_prj/udp_dual_phy1g2.runs/impl_1/wrapper_1g.bit`
> (FCS LSB-first + ICMP 校验和修复 + ap_uint<10>/uint16_t 索引修复 + **rx_fifo 4096/3900**)。
> 收官破案 (2000B 第4段错位): 根因 = wrapper `rx_fifo` 溢出 (2048深/1900门限 顶不住
> 4段背靠背, DUT 排空 ~1字节/20周期), 非 HLS 竞态; 修复 2048→4096/1900→3900。
> 权威分析见 RX_FIFO_OVERFLOW_ANALYSIS.md。
> 验证前提: 网线插 **ETH1** 口 (k720 demo 引脚组), 另一端接 PC 有线网卡 (Killer E5000B,
> 192.168.100.1/24)。**ping 实验必须确认走有线网卡** — WLAN 同网段会造成假阳性。

## 验证顺序 (全部 ✅, 2026-08-23)

1. ✅ **UART console**: `?mac` = 00:0A:35:01:FE:C0, `?ip` = 192.168.100.2, `?stat`, `?net`
2. ✅ **ping**: 4/4 回复 0ms (TTL=128)
3. ✅ **UDP 8080 echo**: 64B / 512B PASS (根因 = `ap_uint<9>` 截断 TX_UDP_BASE=512→0
   + `uint8_t wi` 索引溢出, 均已修)
4. ✅ **TCP 7 echo**: 25B / 1608B / **2000B×3** PASS (2000B 根因 = wrapper rx_fifo 溢出,
   修复 4096/3900)
5. ⬜ **ping 大包**: `ping -l 1000 -f 192.168.100.2` (可选加压项, 未测)
6. 一键回归: `py_net_test.py` (anaconda python `/c/Users/zhxue/anaconda3/python.exe`) —
   覆盖 1-4 项共 7 个用例
   (PowerShell 单项脚本: `udp_console_test.ps1` / `ping_test.ps1` / `echo_test.ps1` / `tcp_test.ps1`)

## 板级探针 (net_stats, 网络调试主力)

| 命令 | 内容 |
|------|------|
| `?net` | rx/tx 帧计数 + 占用率 R/T + 锁 L + ready Y |
| `?txd` | 桥后 TX 首帧前 16B 捕获 (冻结) |
| `?rxd` | 线侧 RX 首帧前 8B 捕获 |
| `?raw` | IP 原始流捕获 |

## PC 侧抓包 (判定 FPGA 帧是否到达的权威手段)

```powershell
pktmon start --comp 117 --flags 0x20 -f D:\repo\ECO\udp_hls_eco\pk.etl
# ... 等 20-30s ...
pktmon stop
pktmon format pk.etl -o pk.txt
```

- **混杂模式必须** (board IP 不是本机 IP, 不混杂看不到)
- 判定: txt 里出现 `00:0A:35:01:FE:C0` 或 demo ARP (who-has 192.168.0.3) = FPGA 帧到达
- **网卡静默丢弃 (pktmon 零包) 的三大原因**: runt <64B / FCS 错 / 字节错位 —
  出现零包时依次排查。FCS 铁律: 本板链只接受 **LSB-first 线上字节序** (以 demo 位流
  线上字节 CA A3 F9 63 为基准), 且要拿权威值**全帧逐字节**比对, 不要用 FPGA 自己的
  CRC 验证自己 (自证闭环两重漏过: 多项式 + 字节序)

## 对照实验 (最快定位"设计问题 vs 物理问题")

烧 k720 demo 位流 (DEMO/k720_rgmii_ethernet1) 到同板同线: pktmon 应见 ~1.1/s 的
demo ARP 帧 (00:0A:35:01:FE:C0, who-has 192.168.0.3)。demo 通而我们不通 →
设计问题; demo 也不通 → 物理链路问题 (网线/口/PHY)。

## 工程文件速查

- 主位流: `vivado_prj/udp_dual_phy1g2.runs/impl_1/wrapper_1g.bit` (生产位流, 7/7 PASS)
- 最小变体: `vivado_prj/udp_minreg.runs/impl_1/wrapper_min.bit` (CRC 修复 + 寄存输出级)
- 烧录: `_run_prog_caller.bat` / `program_eco.tcl` (JTAG 1MHz 自动)
- 串口: COM8 9600-8N1; `net_test.ps1` (探针) / `udp_console_test.ps1` (console)
- 网卡计数: `nic_stats.ps1`; 链路状态: `check_link.ps1`
- ILA 调试: `wrapper_1g_ila.v` + `run_vivado_ila.bat` (工程 `vivado_ila_prj/`) +
  `ila_capture.bat` + `ila_analyze2.py`
- xsim RTL 仿真: `tb_udp_echo.v` (DUT-only) / `tb_wrapper_rx.v` (FIFO+DUT 复现溢出),
  刺激 `gen_stim.py`, 校验 `parse_resp.py`

## 已知遗留 (全部基本功能已通后的可选项)

- 2000B 修复是治标 (rx_fifo 4096 约容 ≤6 段): 治本 = 提升 DUT 排空速率
  (减外层迭代/加宽位宽), 或按目标突发长度继续加大 FIFO — 后续可选优化
- UDP 周期帧 pacing (~5s/帧 @1G, 可接受)
- TCP 健壮性 (重传/RST/乱序/选项) 路径未覆盖 — 基本 echo 已验证, 丢包场景见
  MIGRATION_K7325T.md §3 清单
- ping 大包 (`ping -l 1000 -f`) 未测
