# 网络验证指南 (ETH1 / RTL8211E-VL)

> 2026-08-18 更新。UART console、RX 通路已验证; demo bitstream 同板同线可通 (每秒 1 帧
> ARP 可达 PC); 当前卡点: **本项目任何变体位流的 TX 帧到不了 PC** — 已破案为 demo 克隆
> 生成器 FCS 位序错 (PORT_NOTES 2026-08-18 上午节), 修复版板级复核中。
> 验证前提: 网线插 **ETH1** 口 (k720 demo 引脚组), 另一端接 PC 有线网卡 (Killer E5000B,
> 192.168.100.1/24)。**ping 实验必须确认走有线网卡** — WLAN 同网段会造成假阳性。

## 验证顺序 (TX 修复确认后执行)

1. **UART console 存活**: `udp_console_test.ps1` → `?mac` 返回 `00:0A:35:01:FE:C0`,
   `?ip` 返回 `192.168.100.2`
2. **ping**: `ping_test.ps1` (ping 192.168.100.2, TTL=128, <1ms = 里程碑)
3. **ping 大包**: `ping -l 1000 -f 192.168.100.2` (不分片)
4. **UDP 8080 echo**: `echo_test.ps1` (System.Net.Sockets.UdpClient, "hello ECO")
5. **TCP 7 echo**: `tcp_test.ps1` / `Test-NetConnection 192.168.100.2 -Port 7`
6. 全程 UART 观察: `?stat` (收发包计数), 探针 `?net` (线侧计数)

## 板级探针 (net_stats, 网络调试主力)

| 命令 | 内容 |
|------|------|
| `?net` | rx/tx 帧计数 + 占用率 R/T + 锁 L + ready Y |
| `?txd` | 桥后 TX 首帧前 16B 捕获 (冻结) |
| `?rxd` | 线侧 RX 首帧前 8B 捕获 |
| `?raw` | IP 原始流捕获 |

## PC 侧抓包 (判定 FPGA 帧是否到达的权威手段)

```powershell
pktmon start --comp 117 --flags 0x20 -f D:\repo\ECO\udp_hls_eco\pk.etl
# ... 等 20-30s ...
pktmon stop
pktmon format pk.etl -o pk.txt
```

- **混杂模式必须** (board IP 不是本机 IP, 不混杂看不到)
- 判定: txt 里出现 `00:0A:35:01:FE:C0` 或 demo ARP (who-has 192.168.0.3) = FPGA 帧到达
- **网卡静默丢弃 (pktmon 零包) 的三大原因**: runt <64B / FCS 错 / 字节错位 —
  出现零包时依次排查, FCS 要拿 zlib/802.3 权威值字节级比对, 不要用 FPGA 自己的 CRC 验证自己

## 对照实验 (最快定位"设计问题 vs 物理问题")

烧 k720 demo 位流 (DEMO/k720_rgmii_ethernet1) 到同板同线: pktmon 应见 ~1.1/s 的
demo ARP 帧 (00:0A:35:01:FE:C0, who-has 192.168.0.3)。demo 通而我们不通 →
设计问题; demo 也不通 → 物理链路问题 (网线/口/PHY)。

## 工程文件速查

- 主位流: `vivado_prj/udp_dual_phy1g2.runs/impl_1/wrapper_1g.bit` (重建中, 见 PORT_NOTES)
- 最小变体: `vivado_prj/udp_minreg.runs/impl_1/wrapper_min.bit` (CRC 修复 + 寄存输出级)
- 烧录: `program_eco.tcl` (JTAG 1MHz 自动)
- 串口: COM8 9600-8N1; `net_test.ps1` (探针) / `udp_console_test.ps1` (console)
- 网卡计数: `nic_stats.ps1`; 链路状态: `check_link.ps1`

## 已知遗留 (TX 打通后处理)

- msg_stream CDC FIFO 未做 (UART `?stat` 目前是占位响应)
- UDP 周期帧 pacing (~5s/帧 @1G, 可接受)
- TCP 重传/RST 路径 (丢包场景, 加压前修)
